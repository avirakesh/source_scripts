
if [[ $XDG_SESSION_TYPE == "wayland" ]]; then
    alias code="command code --ozone-platform=wayland"
fi;

alias coden="code -n"

# micro
alias mi="micro"
alias mir="micro -readonly on"

# tmux
alias tmat="tmux attach -t"
alias tmnt="tmux new -t"
